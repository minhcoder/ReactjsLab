import { COMMENTS } from "../shared/comments";
import { DISHES } from "../shared/dishes";

export const Comments=(state=DISHES,action)=>{
    switch(action.type){
        default:
            return state;
    }
}